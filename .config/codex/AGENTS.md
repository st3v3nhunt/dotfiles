# Global AGENTS.md

## Working agreements

- Use relative paths in markdown files to reference other files in the repo
- Keep `AGENTS.md` short and operational; avoid duplicating facts an agent can infer by reading the repository
- Prefer putting project overview, inventories, and user-facing documentation in `README.md` rather than `AGENTS.md`
